import { lte } from 'github-search/helpers/lte';
import { module, test } from 'qunit';

module('Unit | Helper | lte');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lte([42]);
  assert.ok(result);
});
